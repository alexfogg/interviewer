%h2 Analytics

- if @auth.present?
  %table
    %tr
      %th Username
      %th Last Date
      %th Last Score
      %th Avg Score
      %th Number of Attempts
    - @interview.progresses.each do |progress|
      %tr
        %td= progress.user.name
        %td= progress.created_at
        %td= number_to_percentage(@interview.progresses.last.last_score)
        %td= number_to_percentage(@interview.avg_score)
        %td= @interview.progresses.count

#interviewchart